namespace Zen.Trunk.Storage.Query
{
    public class SchemaSymbolScope : ChildSymbolScope
    {
        public SchemaSymbolScope(string name, SymbolScope parentScope) : base(name, parentScope)
        {
        }
    }
}